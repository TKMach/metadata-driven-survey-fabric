# Security

Secrets must be supplied at runtime (for example through Azure Key Vault). Do not commit OAuth credentials, tokens, production IDs, private configuration, participant data, or IP addresses. Public examples use synthetic identifiers only.
