# Monitoring

Run logging captures timestamps, active surveys, discovery and detail failures, response counts, prepared rows, run status, and duration. Health views can summarize run, survey, and platform status.
