# Architecture

Fabric orchestrates a metadata-driven API ingestion workload. Runtime secrets support OAuth, registry metadata controls work selection, and normalized results are persisted to Delta tables. The public version replaces all production identifiers with generic configuration.
